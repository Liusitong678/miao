var liusitong678 = {
  isNull:function(val){
    if (val === null){
      return ture
    }else{
      return false
    }
  },
  isNaN: function(val){
    if (val !== val){
      return true
    }else{
      return false
    }
  },
  /**
   * 将数组分成多组，每组有size个元素
   *
   * @param ary {Array} 需要拆分的数组
   * @param size {Nunber} 拆分的大小
   * @returns {Array[][]}
   */
  chunk: function(){

  },
}
